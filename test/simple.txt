CREATE TABLE test.mock1 ('id','first','last') file='/home/nd/repos/wlddb/test/test.mock.csv' column=',' strict=true;
CREATE TABLE test.mock2 ('id','first','last') file='/home/nd/repos/wlddb/test/test.mock.csv' column=',' strict=true;
CREATE TABLE test.mock3 ('id','first','last') file='/home/nd/repos/wlddb/test/test.mock.csv' column=',' strict=true;
USE test;

SELECT 
    DISTINCT 
    t1.id as id1,       --identifier
    t2.id as id2,       --identifier
    t3.id as id3,       --identifier
    t1.first as first1, --identifier
    t2.first as first2, --identifier
    t3.first3 as first3  --identifier
    
    FROM            test.mock1 as t1
    JOIN                 mock1 as t2 ON -a-1*5+5=b and -c=sc-1
    JOIN                 mock2 as t3 ON -a-1*5+5=b and -c=sc-1
    
    
    WHERE 
        res.first_name='bob'
        or 1  or 2
        or x=1.54
        and  id=0x04F 
        or id=0x04A
        or x =-1-1
        
    GROUP BY id1,id2,id3
    ORDER BY id1,id2,id3
    LIMIT 1, 11;
        