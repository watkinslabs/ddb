CREATE TABLE test.mock1 ('id','first','last') file='/home/nd/repos/wlddb/test/test.mock.csv' column=',' strict=true;
CREATE TABLE test.mock2 ('id','first','last') file='/home/nd/repos/wlddb/test/test.mock.csv' column=',' strict=true;
CREATE TABLE test.mock3 ('iid','first','last') file='/home/nd/repos/wlddb/test/test.mock.csv' column=',' strict=true;
USE test;

SELECT 
    DISTINCT 
    1 as prefix,
    t1.id as id1,       --identifier
    t2.id as id2,       --identifier
    t3.iid as id3,       --identifier
    t1.first as first1, --identifier
    t2.first as first2, --identifier
    t3.first as first3  --identifier
    
    FROM            test.mock1 as t1
    JOIN                 mock1 as t2 ON t2.id=1 
    JOIN                 mock3 as t3 ON t3.iid=1 
    
    --WHERE 
       /*        
        1+15+6+8+456+123123+4=15+6+8+456+123123+4+11
             
        and 2+2+41=4+1+5+35
        and 5
        and 4+1
        or 1=1+2
        or 2+3
        and 5=-1+6-1
        or 3.4+4.1=3+10-0.5-5
        or 5.2;
       */
        /*
        or "S"+4
        or   t1.id=1.54
        and  t2.id=0x04F 
        or   t1.id=0x04A
        or   t2.id =-1-1
        
    GROUP BY t2.id,t1.id,iid
    ORDER BY t1.id,iid,t2.id
    LIMIT 1, 10;
    
        */